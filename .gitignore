Thumbs.db
desktop.ini
.DS_Store
*.log

# Outils de dev (tests) — jamais déployés
node_modules/
test-results/
playwright-report/
blob-report/
.playwright/
tests/e2e/_*.spec.mjs

# Backups locaux avant corrections (convention audit) — pas dans le dépôt
archives/

# Restitution d'une autre IA (Codex) — hors dépôt tant que non tranché (ajouté par erreur dans 8ca9d47, retiré aussitôt)
audit codex/

# Exports de l'app (sauvegardes JSON, CSV) : données nominatives, jamais dans le dépôt public (audit 2026-09-07, A03)
carnet-eps_*.json
releve_*.csv
recap-eps_*.csv
notes_*.csv
